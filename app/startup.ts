import {bootstrap}    from 'angular2/platform/browser' // import app bootstrapper from angular library
import {AppComponent} from './app.component' // import the Appcomponent(the main Component in your angular app)

// this loads up the AppComponent 
bootstrap(AppComponent);